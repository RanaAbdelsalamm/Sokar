import 'package:flutter/material.dart';

class AppColors {
  // Main blue background for splash and primary buttons
  static const Color primaryBlue = Color(0xFF6BBAEC);
  
  // Clean white for landing page background
  static const Color whiteBackground = Color(0xFFFFFFFF);
  
  // Very dark gray for main titles
  static const Color textMainTitle = Color(0xFF111718);
  
  // Teal-gray for subtitles
  static const Color textSubtitle = Color(0xFF618389);
  
  // Dark gray for card titles
  static const Color textCardTitle = Color(0xFF353531);
  
  // Light gray for card descriptions and footer text
  static const Color textBody = Color(0xFF9CA3AF);
  
  // Pale blue-gray for borders and icons
  static const Color borderLight = Color(0xFFDBE4E6);
  
  // Registration page colors
  static const Color textDarkBlue = Color(0xFF243A5F); // For subtitles and section headers
  static const Color inputBackground = Color(0xFFEDF4FA); // For text field backgrounds
  static const Color textBlack = Color(0xFF000000); // For main titles
  static const Color textDarkGray = Color(0xFF333333); // For input labels
}